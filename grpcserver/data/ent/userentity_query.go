// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"grpcserver/data/ent/predicate"
	"grpcserver/data/ent/userentity"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserEntityQuery is the builder for querying UserEntity entities.
type UserEntityQuery struct {
	config
	ctx        *QueryContext
	order      []userentity.OrderOption
	inters     []Interceptor
	predicates []predicate.UserEntity
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserEntityQuery builder.
func (ueq *UserEntityQuery) Where(ps ...predicate.UserEntity) *UserEntityQuery {
	ueq.predicates = append(ueq.predicates, ps...)
	return ueq
}

// Limit the number of records to be returned by this query.
func (ueq *UserEntityQuery) Limit(limit int) *UserEntityQuery {
	ueq.ctx.Limit = &limit
	return ueq
}

// Offset to start from.
func (ueq *UserEntityQuery) Offset(offset int) *UserEntityQuery {
	ueq.ctx.Offset = &offset
	return ueq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ueq *UserEntityQuery) Unique(unique bool) *UserEntityQuery {
	ueq.ctx.Unique = &unique
	return ueq
}

// Order specifies how the records should be ordered.
func (ueq *UserEntityQuery) Order(o ...userentity.OrderOption) *UserEntityQuery {
	ueq.order = append(ueq.order, o...)
	return ueq
}

// First returns the first UserEntity entity from the query.
// Returns a *NotFoundError when no UserEntity was found.
func (ueq *UserEntityQuery) First(ctx context.Context) (*UserEntity, error) {
	nodes, err := ueq.Limit(1).All(setContextOp(ctx, ueq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userentity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ueq *UserEntityQuery) FirstX(ctx context.Context) *UserEntity {
	node, err := ueq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserEntity ID from the query.
// Returns a *NotFoundError when no UserEntity ID was found.
func (ueq *UserEntityQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ueq.Limit(1).IDs(setContextOp(ctx, ueq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userentity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ueq *UserEntityQuery) FirstIDX(ctx context.Context) int {
	id, err := ueq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserEntity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserEntity entity is found.
// Returns a *NotFoundError when no UserEntity entities are found.
func (ueq *UserEntityQuery) Only(ctx context.Context) (*UserEntity, error) {
	nodes, err := ueq.Limit(2).All(setContextOp(ctx, ueq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userentity.Label}
	default:
		return nil, &NotSingularError{userentity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ueq *UserEntityQuery) OnlyX(ctx context.Context) *UserEntity {
	node, err := ueq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserEntity ID in the query.
// Returns a *NotSingularError when more than one UserEntity ID is found.
// Returns a *NotFoundError when no entities are found.
func (ueq *UserEntityQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ueq.Limit(2).IDs(setContextOp(ctx, ueq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userentity.Label}
	default:
		err = &NotSingularError{userentity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ueq *UserEntityQuery) OnlyIDX(ctx context.Context) int {
	id, err := ueq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserEntities.
func (ueq *UserEntityQuery) All(ctx context.Context) ([]*UserEntity, error) {
	ctx = setContextOp(ctx, ueq.ctx, "All")
	if err := ueq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserEntity, *UserEntityQuery]()
	return withInterceptors[[]*UserEntity](ctx, ueq, qr, ueq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ueq *UserEntityQuery) AllX(ctx context.Context) []*UserEntity {
	nodes, err := ueq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserEntity IDs.
func (ueq *UserEntityQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ueq.ctx.Unique == nil && ueq.path != nil {
		ueq.Unique(true)
	}
	ctx = setContextOp(ctx, ueq.ctx, "IDs")
	if err = ueq.Select(userentity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ueq *UserEntityQuery) IDsX(ctx context.Context) []int {
	ids, err := ueq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ueq *UserEntityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ueq.ctx, "Count")
	if err := ueq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ueq, querierCount[*UserEntityQuery](), ueq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ueq *UserEntityQuery) CountX(ctx context.Context) int {
	count, err := ueq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ueq *UserEntityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ueq.ctx, "Exist")
	switch _, err := ueq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ueq *UserEntityQuery) ExistX(ctx context.Context) bool {
	exist, err := ueq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserEntityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ueq *UserEntityQuery) Clone() *UserEntityQuery {
	if ueq == nil {
		return nil
	}
	return &UserEntityQuery{
		config:     ueq.config,
		ctx:        ueq.ctx.Clone(),
		order:      append([]userentity.OrderOption{}, ueq.order...),
		inters:     append([]Interceptor{}, ueq.inters...),
		predicates: append([]predicate.UserEntity{}, ueq.predicates...),
		// clone intermediate query.
		sql:  ueq.sql.Clone(),
		path: ueq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserEntity.Query().
//		GroupBy(userentity.FieldUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ueq *UserEntityQuery) GroupBy(field string, fields ...string) *UserEntityGroupBy {
	ueq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserEntityGroupBy{build: ueq}
	grbuild.flds = &ueq.ctx.Fields
	grbuild.label = userentity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//	}
//
//	client.UserEntity.Query().
//		Select(userentity.FieldUsername).
//		Scan(ctx, &v)
func (ueq *UserEntityQuery) Select(fields ...string) *UserEntitySelect {
	ueq.ctx.Fields = append(ueq.ctx.Fields, fields...)
	sbuild := &UserEntitySelect{UserEntityQuery: ueq}
	sbuild.label = userentity.Label
	sbuild.flds, sbuild.scan = &ueq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserEntitySelect configured with the given aggregations.
func (ueq *UserEntityQuery) Aggregate(fns ...AggregateFunc) *UserEntitySelect {
	return ueq.Select().Aggregate(fns...)
}

func (ueq *UserEntityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ueq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ueq); err != nil {
				return err
			}
		}
	}
	for _, f := range ueq.ctx.Fields {
		if !userentity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ueq.path != nil {
		prev, err := ueq.path(ctx)
		if err != nil {
			return err
		}
		ueq.sql = prev
	}
	return nil
}

func (ueq *UserEntityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserEntity, error) {
	var (
		nodes = []*UserEntity{}
		_spec = ueq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserEntity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserEntity{config: ueq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ueq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ueq *UserEntityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ueq.querySpec()
	_spec.Node.Columns = ueq.ctx.Fields
	if len(ueq.ctx.Fields) > 0 {
		_spec.Unique = ueq.ctx.Unique != nil && *ueq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ueq.driver, _spec)
}

func (ueq *UserEntityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userentity.Table, userentity.Columns, sqlgraph.NewFieldSpec(userentity.FieldID, field.TypeInt))
	_spec.From = ueq.sql
	if unique := ueq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ueq.path != nil {
		_spec.Unique = true
	}
	if fields := ueq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userentity.FieldID)
		for i := range fields {
			if fields[i] != userentity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ueq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ueq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ueq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ueq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ueq *UserEntityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ueq.driver.Dialect())
	t1 := builder.Table(userentity.Table)
	columns := ueq.ctx.Fields
	if len(columns) == 0 {
		columns = userentity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ueq.sql != nil {
		selector = ueq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ueq.ctx.Unique != nil && *ueq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ueq.predicates {
		p(selector)
	}
	for _, p := range ueq.order {
		p(selector)
	}
	if offset := ueq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ueq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserEntityGroupBy is the group-by builder for UserEntity entities.
type UserEntityGroupBy struct {
	selector
	build *UserEntityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uegb *UserEntityGroupBy) Aggregate(fns ...AggregateFunc) *UserEntityGroupBy {
	uegb.fns = append(uegb.fns, fns...)
	return uegb
}

// Scan applies the selector query and scans the result into the given value.
func (uegb *UserEntityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uegb.build.ctx, "GroupBy")
	if err := uegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserEntityQuery, *UserEntityGroupBy](ctx, uegb.build, uegb, uegb.build.inters, v)
}

func (uegb *UserEntityGroupBy) sqlScan(ctx context.Context, root *UserEntityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uegb.fns))
	for _, fn := range uegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uegb.flds)+len(uegb.fns))
		for _, f := range *uegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserEntitySelect is the builder for selecting fields of UserEntity entities.
type UserEntitySelect struct {
	*UserEntityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ues *UserEntitySelect) Aggregate(fns ...AggregateFunc) *UserEntitySelect {
	ues.fns = append(ues.fns, fns...)
	return ues
}

// Scan applies the selector query and scans the result into the given value.
func (ues *UserEntitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ues.ctx, "Select")
	if err := ues.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserEntityQuery, *UserEntitySelect](ctx, ues.UserEntityQuery, ues, ues.inters, v)
}

func (ues *UserEntitySelect) sqlScan(ctx context.Context, root *UserEntityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ues.fns))
	for _, fn := range ues.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ues.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ues.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
